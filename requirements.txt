# Core runtime — everything the app needs to run locally with no cloud account.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
httpx>=0.27
reportlab>=4.0

# Optional extras (not required to run):
#   requirements-ml.txt   -> dense sentence-transformers retrieval backend
#   requirements-dev.txt  -> pytest for the test suite
